  <div id="modal_error">
    <% if ldap_attribute.errors.any? %>
      <div id="error_explanation" class="alert alert-danger" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <strong><%= pluralize(ldap_attribute.errors.count, "error") %> prohibited this ldap_attribute from being saved:</strong>

        <ul>
        <% ldap_attribute.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  </div>
