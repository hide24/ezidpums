  <div id="modal_error">
    <% if idp_attribute.errors.any? %>
      <div id="error_explanation" class="alert alert-danger" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <strong><%= pluralize(idp_attribute.errors.count, "error") %> prohibited this idp_attribute from being saved:</strong>

        <ul>
        <% idp_attribute.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  </div>
